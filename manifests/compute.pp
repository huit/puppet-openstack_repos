# Class:: openstack_repos::compute
#
#
class openstack_repos::compute (
  $local_mirrors,
) {

  notify { 'This is just a stub, no openstack_repos::compute class yet.': }

} # Class:: openstack_repos::compute
